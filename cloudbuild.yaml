steps:
- name: 'gcr.io/cloud-builders/docker'
  script: |
    docker build -t europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/metabase/metabase .
  automapSubstitutions: true
images:
- 'europe-west1-docker.pkg.dev/$PROJECT_ID/cloud-run-source-deploy/metabase/metabase'
timeout: 3600
